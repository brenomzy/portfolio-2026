# Project design rules

- Never use eyebrow text above section headings. Establish hierarchy through the heading, layout, spacing, and content itself.
- Small labels inside components may use the reusable `c-meta-label` style; they must not introduce or label a section.
