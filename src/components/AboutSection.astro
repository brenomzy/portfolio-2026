---
/*
 * c-about — the homepage About section (condensed). One step deeper than the
 * hero: a short approach-led blurb + portrait + a compact tool row (colored
 * app-icon tiles, matching /about), with a "More about me" link into /about.
 *
 * Reveal language mirrors WorkSection: [data-reveal] + .js-about-reveal items
 * park hidden (CSS guard in global.css) and slide up via a ScrollTrigger.batch
 * when the section scrolls into view.
 */
import { ABOUT } from "../lib/about";

// A compact subset of the toolset for the home teaser; the full grid lives on
// the /about page.
const tools = ABOUT.tools.slice(0, 8);
---

<section class="c-about" id="about" aria-label="About">
	<div class="c-about__grid">
		<div class="c-about__content">
			<p class="c-about__eyebrow js-about-reveal" data-reveal>{ABOUT.home.heading}</p>
			<h2 class="c-about__lead js-about-reveal" data-reveal>{ABOUT.home.lead}</h2>
			<p class="c-about__body js-about-reveal" data-reveal>{ABOUT.home.body}</p>

			<ul class="c-about__tools js-about-reveal" data-reveal aria-label="Tools I use">
				{
					tools.map((t) => (
						<li class="c-about__tool" title={t.name}>
							<span class="c-about__tool-tile" style={`--tile: ${t.color}`}>
								<span
									class="c-about__tool-glyph"
									style={`--icon: url(/tools/${t.icon}); --glyph: ${t.darkGlyph ? "#10151c" : "#fff"}`}
									aria-hidden="true"
								/>
							</span>
							<span class="c-about__sr">{t.name}</span>
						</li>
					))
				}
			</ul>

			<a class="c-about__more js-about-reveal" data-reveal href="/about">
				More about me
				<svg
					class="c-about__arrow"
					viewBox="0 0 256 256"
					fill="currentColor"
					aria-hidden="true"
				>
					<path d="M221.66,133.66l-72,72a8,8,0,0,1-11.32-11.32L196.69,136H40a8,8,0,0,1,0-16H196.69L138.34,61.66a8,8,0,0,1,11.32-11.32l72,72A8,8,0,0,1,221.66,133.66Z" />
				</svg>
			</a>
		</div>

		<div class="c-about__portrait js-about-reveal" data-reveal>
			<img
				src={ABOUT.portrait.src}
				alt={ABOUT.portrait.alt}
				width="600"
				height="800"
				loading="lazy"
			/>
		</div>
	</div>
</section>

<style>
	.c-about {
		max-width: var(--container-content);
		margin-inline: auto;
		padding: var(--spacing-2xl) var(--spacing-m);
	}
	.c-about__grid {
		display: grid;
		grid-template-columns: minmax(0, 1.4fr) minmax(0, 1fr);
		gap: var(--spacing-2xl);
		align-items: center;
	}
	.c-about__content {
		display: grid;
		gap: var(--spacing-s);
		/* Comfortable reading measure inside the wide container. */
		max-width: 46ch;
	}
	.c-about__eyebrow {
		font-size: 0.625rem;
		text-transform: uppercase;
		letter-spacing: 0.18em;
		color: var(--accent-ink);
		font-weight: 600;
	}
	.c-about__lead {
		font-size: var(--text-step-3);
		text-wrap: pretty;
	}
	.c-about__body {
		color: var(--text-muted);
	}

	/* Tool row — colored app-icon tiles (matches the /about Tool stack). */
	.c-about__tools {
		display: flex;
		flex-wrap: wrap;
		align-items: center;
		gap: var(--spacing-2xs);
		list-style: none;
		padding: 0;
		margin-top: var(--spacing-2xs);
	}
	.c-about__tool {
		display: inline-flex;
	}
	/* App-icon tile: brand-colour square, white (or dark) brand glyph masked on. */
	.c-about__tool-tile {
		display: grid;
		place-items: center;
		width: 2.75rem;
		height: 2.75rem;
		border-radius: var(--radius-md);
		background: var(--tile);
		box-shadow: var(--shadow-1);
		transition: transform 0.25s var(--ease-out-expo);
	}
	.c-about__tool-glyph {
		width: 56%;
		height: 56%;
		background-color: var(--glyph);
		-webkit-mask: var(--icon) center / contain no-repeat;
		mask: var(--icon) center / contain no-repeat;
	}
	@media (hover: hover) {
		.c-about__tool:hover .c-about__tool-tile {
			transform: translateY(-3px);
		}
	}
	/* Visually-hidden tool name (the tile is title-tooltipped). */
	.c-about__sr {
		position: absolute;
		width: 1px;
		height: 1px;
		padding: 0;
		margin: -1px;
		overflow: hidden;
		clip: rect(0 0 0 0);
		white-space: nowrap;
		border: 0;
	}

	.c-about__more {
		display: inline-flex;
		align-items: center;
		gap: 0.4em;
		margin-top: var(--spacing-2xs);
		width: fit-content;
		font-size: var(--text-step--1);
		font-weight: 560;
		text-decoration: none;
		color: var(--text);
	}
	.c-about__arrow {
		width: 1.05em;
		height: 1.05em;
		transition: transform 0.3s var(--ease-out-expo);
	}
	@media (hover: hover) and (prefers-reduced-motion: no-preference) {
		.c-about__more:is(:hover, :focus-visible) {
			color: var(--accent-ink);
		}
		.c-about__more:is(:hover, :focus-visible) .c-about__arrow {
			transform: translateX(0.2em);
		}
	}

	.c-about__portrait {
		overflow: hidden;
		border-radius: var(--radius-md);
		background: var(--thumb-bg);
		box-shadow: var(--shadow-2);
	}
	.c-about__portrait img {
		width: 100%;
		height: 100%;
		object-fit: cover;
		aspect-ratio: 3 / 4;
	}

	@media (max-width: 52rem) {
		.c-about__grid {
			grid-template-columns: 1fr;
			gap: var(--spacing-xl);
		}
		/* Portrait first, then text, on narrow screens. */
		.c-about__portrait {
			order: -1;
			max-width: 22rem;
		}
	}
</style>

<script>
	import { gsap } from "gsap";
	import { ScrollTrigger } from "gsap/ScrollTrigger";
	import { CustomEase } from "gsap/CustomEase";
	import { whenRevealable } from "../lib/page-reveal";

	gsap.registerPlugin(ScrollTrigger, CustomEase);

	const reduced = matchMedia("(prefers-reduced-motion: reduce)");
	// Reuse the shared premium ease-out (same registry as Work/modal).
	const EASE_OUT =
		CustomEase.get("brand-out") ?? CustomEase.create("brand-out", "0.16,1,0.3,1");

	let triggers: ScrollTrigger[] = [];

	function killTriggers() {
		triggers.forEach((t) => t.kill());
		triggers = [];
	}

	const reveal = (els: HTMLElement | HTMLElement[]) =>
		gsap.to(els, {
			autoAlpha: 1,
			y: 0,
			duration: 0.8,
			ease: EASE_OUT,
			stagger: 0.08,
			overwrite: true,
		});

	function setup() {
		const items = gsap.utils.toArray<HTMLElement>(".js-about-reveal");
		if (!items.length) return;
		killTriggers();

		if (reduced.matches) {
			gsap.set(items, { autoAlpha: 1, y: 0 });
			return;
		}

		// Park hidden + down (the data-reveal CSS guard prevents a pre-JS flash).
		gsap.set(items, { autoAlpha: 0, y: 40 });

		// Reveal anything already on-screen at load; scroll-reveal the rest per
		// item (ScrollTrigger.batch skips items already past the start line).
		const threshold = window.innerHeight * 0.9;
		const onLoad: HTMLElement[] = [];
		items.forEach((el) => {
			if (el.getBoundingClientRect().top < threshold) onLoad.push(el);
			else
				triggers.push(
					ScrollTrigger.create({
						trigger: el,
						start: "top 85%",
						once: true,
						onEnter: () => reveal(el),
					}),
				);
		});
		// Defer the in-view reveals + the layout-heavy ScrollTrigger.refresh until
		// the cross-fade settles, so neither competes with the page transition.
		// Resolves immediately on a fresh load, so that path is unchanged.
		whenRevealable().then(() => {
			if (onLoad.length) reveal(onLoad);
			ScrollTrigger.refresh();
		});
	}

	document.addEventListener("astro:page-load", setup);
	document.addEventListener("astro:before-swap", killTriggers);
</script>
